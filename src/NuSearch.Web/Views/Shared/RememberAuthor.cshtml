@using NuSearch.Web.Models
@model SearchForm

@if (Model.Authors != null && Model.Authors.Length > 0)
{
	@foreach (var a in Model.Authors)
	{
		<input name="@Html.NameFor(m=>m.Authors).ToLowerInvariant()" type="hidden" value="@a"/>
	}
	
}
